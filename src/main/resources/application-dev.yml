spring:
  datasource:
    url: jdbc:mysql://localhost:5508/sfg_dev?serverTimezone=UTC
    username: sfg_dev_user
    password: dev_pass
  jpa:
    hibernate:
      ddl-auto: validate
    database-platform: org.hibernate.dialect.MySQL5Dialect
    database: mysql
    show-sql: true
    properties:
      javax:
        persistence:
          schema-generation:
            create-source: metadata
            scripts:
              action: create
              create-target: recipe_database_create_sql
